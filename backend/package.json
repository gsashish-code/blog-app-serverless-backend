{
  "name": "backend",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "postinstall": "prisma generate dev",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@gsashish/medium-common": "^1.0.0",
    "@prisma/client": "^6.6.0",
    "@prisma/extension-accelerate": "^1.3.0",
    "bcryptjs": "^3.0.2",
    "hono": "^4.7.7",
    "prisma": "^6.6.0"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
